<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema base -->
    <style name="Base.Theme.TiendaOnline" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/purple_700</item>
        <item name="colorSecondary">@color/teal_200</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <!-- Tema general -->
    <style name="Theme.TiendaOnline" parent="Base.Theme.TiendaOnline" />

    <!-- Tema sin ActionBar -->
    <style name="Theme.TiendaOnline.NoActionBar" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

</resources>
